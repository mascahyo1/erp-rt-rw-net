# Workflow Coding & Test — v3 → Dilebur ke STANDARDS.md (v4 Single Source)

> **File ini legacy — jangan edit di sini.**
> Kontrak build-time v3 (lifecycle/state/selector solid, headed E2E per-langkah assert + screenshot/network/video, definisi selesai) telah **dilebur menjadi `STANDARDS.md §1, §7, §8`** (v4 Single Source of Truth) + `workflow.md` (ringkas 1 halaman).
>
> **Baca sekarang:**
> - `STANDARDS.md` — sumber tunggal (11 section, §1 kontrak build-time, §7 headed E2E per-langkah, §8 DoD)
> - `workflow.md` — ringkas 1 halaman untuk harian
> - `dokumentasi/CONVENTIONS.md` — Hybrid Inertia+AJAX detail
> - `AGENTS.md` § Workflow + `.kilo/AGENT-WORKFLOW.md` + `.kilo/rules.md`
>
> **Status penerapan (2026-08-29):** `STANDARDS.md` & `workflow.md` sudah dibuat di root, `scripts/check-testing-standards.cjs` meng-enforce kontrak ke semua testing (Playwright primary, PHPUnit backend, Dusk legacy).
>
> Riwayat: v3 menaikkan "syarat sebelum testing" jadi kontrak build-time — prinsip tetap dipertahankan di v4. Isi asli v3 diarsipkan di git history (`git log -- dokumentasi/"workflow coding dan test.md"`).
